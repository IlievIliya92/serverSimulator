#!/bin/bash

source ./scripts/envClient
source ./scripts/addtopathClient

printf "Commands imported:\n\n"
printf "buildClient - build the client\n"
printf "buildClientLib - build shared .so for the client lib\n"
